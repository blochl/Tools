# A script for restoring an external DP display upon re-plug

Some laptops (e.g. DELL Latitude 7420) do not restore an external display
automatically after being unplugged and then re-plugged. To solve this, no
reboot is needed - just run this script upon re-plugging the display.
